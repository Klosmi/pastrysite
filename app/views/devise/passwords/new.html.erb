<<<<<<< HEAD
<div class="forgotpassword-password">
    <h2 class="forgotpassword-title">Forgot your password?</h2>
    <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
    <%= f.error_notification %>
    <div class="form-inputs">
        <%= f.input :email,
||||||| merged common ancestors
<h2 class="forgotpw">Forgot your password?</h2>

<%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email,
=======
<div class="forgotpassword-password">
    <h2 class="forgotpassword-title">Forgot your password?</h2>
    <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
    <%= f.error_notification %>
    <div class="form-inputs d-flex justify-content-center">

        <%= f.input :email,
>>>>>>> loginokay-styling
                required: true,
                autofocus: true,
                input_html: { autocomplete: "email" } %>
<<<<<<< HEAD
    </div>
    <div class="form-actions forgotpassword-button">
        <%= f.button :submit, "Send me reset password instructions" %>
    </div>
    <% end %>
    <div class="shared-links">
        <%= render "devise/shared/links" %>
    </div>
</div>
||||||| merged common ancestors
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Send me reset password instructions" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
=======
    </div>
    <div class="form-actions forgotpassword-button d-flex justify-content-center mb-5">
        <%= f.button :submit, "Send me reset password instructions", class: "login-button" %>
    </div>
    <% end %>
    <div class="shared-links">
        <%= render "devise/shared/links" %>
    </div>
</div>
>>>>>>> loginokay-styling
